"""The `plan` command family: the periodization the athlete trains towards.

`generate.py` writes a new version and previews it, `show.py` reads the current one,
`versions.py` compares and rolls back between them, `feedback.py` keeps the log of
notes the next `plan generate` reads, and `parser.py` is the argparse tree.
"""
