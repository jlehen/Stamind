.claude
TODO
# Top-level only: a bare "science" would also swallow stamind/science/, the tracked built-ins
/science/

# Virtual environment
venv/
.venv/

# Credentials & configs
config.yaml
service_account.json
# Garmin OAuth token stores — default .garminconnect beside each config, any depth
.garminconnect/

# Local database, and the hand-made snapshots taken before risky migrations
stamind.db
stamind.db.bak*
stamind.db.freshstart
stamind.db-journal
stamind.db-wal
stamind.db-shm

# Logs, and the gym logs the bot writes before ingesting them (DESIGN_gym_logger.md §6)
logs
gym_logs/
# The note a /restart leaves for the next worker (DESIGN_bot_restart.md §5.2)
restart_chat

# Chart output (sm progress --chart)
progress.png

# Python caches
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ipynb_checkpoints/

# vim swap files (one was committed by accident)
.*.swp
# The gym-logger prototype bot (DESIGN_gym_logger.md): its own config and database
config.proto.yaml
stamind.proto.db*
